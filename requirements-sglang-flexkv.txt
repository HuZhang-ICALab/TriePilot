# Key package pins observed in FlexKV's remote conda env `sglang-flex`.
# Install the full SGLang stack from third_party/sglang_flex/python when running
# on Linux/A100. Native Windows support is not assumed.
torch==2.9.1
triton==3.5.1
transformers==4.57.6
numpy==2.2.6
PyYAML==6.0.3
scikit-learn==1.7.2
matplotlib==3.10.8

